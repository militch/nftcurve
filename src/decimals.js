import Decimal from 'decimal.js';

const BIG_ZERO = new Decimal(0);
const BIG_ONE = new Decimal(1);
const BIG_TWO = new Decimal(2);
const BIG_TEN = new Decimal(10);

export {
  BIG_ZERO,
  BIG_ONE,
  BIG_TWO,
  BIG_TEN,
}
